{
  "name": "ongame",
  "description": "ongame — AI game-making: /make-game takes a concept through a personalized, approval-gated phase pipeline to a playable, deployable game. This plugin is a thin shim; the self-updating ongame-cli binary it launches does the real work.",
  "version": "1.7.0",
  "author": {
    "name": "ongame"
  },
  "mcpServers": {
    "ongame": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/ongame-launcher",
      "args": [
        "mcp"
      ]
    },
    "playwright": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/ongame-launcher",
      "args": [
        "playwright"
      ]
    }
  }
}
